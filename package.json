{
  "name": "maka",
  "private": true,
  "version": "0.1.0",
  "description": "Maka monorepo for voice agent SDK and services",
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "packageExtensions": {
      "webrtc-adapter@*": {
        "peerDependencies": {
          "ws": "^8.0.0"
        }
      }
    }
  }
}
